---
applications:
  - name: gdrive
    routes:
      - route: idva-gdrive-((ENVIRONMENT)).apps.internal
    memory: ((MEMORY))
    instances: ((INSTANCES))
    buildpacks:
      - python_buildpack
    command: uvicorn gdrive.main:app --host 0.0.0.0 --port $PORT
    services:
      - gdrive
      - outbound-proxy
    env:
      ES_HOST: identity-idva-es-proxy-((ENVIRONMENT)).apps.internal
      ES_PORT: 8080
      QUALTRICS_API_URL: idva-qualtrix-((ENVIRONMENT)).apps.internal
      QUALTRICS_API_PORT: 8080
      NO_PROXY: .apps.internal